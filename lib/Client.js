// TODO: node-fetch
const fetch = require('node-fetch')

class Client {
  constructor(otps = {}) {
    let {host} = opts
    if (!host) throw Error('Host is mandatory')
    this.host = host

  }

  fetch () {
    // TODO: fetch({host}).then(...)

  }

}
